spring:
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/point_example
    username: root
    password: hramst0618@
  kafka:
    bootstrap-servers: localhost:9092
    producer:
      key-serializer: org.apache.kafaka.common.serialization.StringSerializer
      value-serializer: org.spingframework.kafka.support.serializer.JsonSerializer
    consumer:
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.use.type.headers: false
        spring.json.trusted.packages: "*"
server:
  port: 8081
